services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: always
    networks:
      - ui-designer-net

  db:
    image: postgres:16-alpine
    container_name: ui-designer.db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hello_ui
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d hello_ui"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: always
    networks:
      - ui-designer-net

  db-init:
    build:
      context: .
      dockerfile: Dockerfile.db-init
    container_name: ui-designer.db-init
    image: ui-designer-db-init:latest
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/hello_ui
    depends_on:
      db:
        condition: service_healthy
    restart: "no"
    networks:
      - ui-designer-net

  drawboard:
    build:
      context: ./drawBoard
      dockerfile: Dockerfile
    container_name: ui-designer.drawboard
    image: ui-designer-drawboard:latest
    ports:
      - "54320:3500"
    environment:
      TZ: Europe/Helsinki
      NODE_ENV: production
    restart: always
    networks:
      - ui-designer-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        NEXT_PUBLIC_DRAWBOARD_URL: ${NEXT_PUBLIC_DRAWBOARD_URL}
        NEXT_PUBLIC_WEBSOCKET_URL: ${NEXT_PUBLIC_WEBSOCKET_URL}
        NEXT_PUBLIC_BASE_PATH: ${NEXT_PUBLIC_BASE_PATH}
    container_name: ui-designer.app
    image: ui-designer-app:latest
    ports:
      - "54322:3000"
    volumes:
      - ./logs:/app/logs
    env_file:
      - .env.production
    environment:
      DATABASE_URL: postgresql://postgres:postgres@db:5432/hello_ui
      DB_CLIENT: postgres
      TZ: Europe/Helsinki
      NODE_ENV: production
      WS_SERVICE_TOKEN: ${WS_SERVICE_TOKEN:-ws-service-secret}
      ADMIN_EMAIL: "raitsu11@gmail.com"
    depends_on:
      db-init:
        condition: service_completed_successfully
      drawboard:
        condition: service_started
      redis:
        condition: service_healthy
    restart: always
    networks:
      - ui-designer-net

  ws-server:
    build:
      context: ./ws-server
      dockerfile: Dockerfile
    container_name: ui-designer.ws
    image: ui-designer-ws:latest
    ports:
      - "54323:8080"
    env_file:
      - .env.production
    environment:
      PORT: 8080
      API_BASE_URL: http://app:3000
      DATABASE_URL: postgresql://postgres:postgres@db:5432/hello_ui
      CORS_ORIGIN: ${NEXT_PUBLIC_APP_URL}
      TZ: Europe/Helsinki
      NODE_ENV: production
      WS_SERVICE_TOKEN: ${WS_SERVICE_TOKEN:-ws-service-secret}
    depends_on:
      app:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8080/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: always
    networks:
      - ui-designer-net

volumes:
  pgdata:

networks:
  ui-designer-net:
    driver: bridge
